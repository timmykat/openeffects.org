.panel.panel-default
  .panel-body
    %h4.text-primary Discussion
    - if @comments.blank?
      %p No comments on this change yet
    - else
      - @comments.each do |comment|
        = render :partial => 'comments/comment', locals: { :comment => comment }

    - if current_user    
      = render 'comments/form'